<table>
<tr>
    <th>Name</th>
    <th> | </th>
    <th>Reason to avoid</th>
</tr>

<% @ingredients.each do |ingredient| %>
    <tr>
        <td align="center"><%= ingredient.name %></td>
        <td>|</td>
        <td align="center"><%= ingredient.reasontoavoid %></td>
        <td>|</td>
        <td><%= link_to 'Edit', edit_ingredient_path(ingredient) %></td>
        <td><%= link_to 'Show', ingredient_path(ingredient) %></td>
        <td><%= link_to 'Delete', ingredient_path(ingredient), method: :delete, data: {confirm: 'Are you sure?' } %></td>
    </tr>
<% end %>

</table>
